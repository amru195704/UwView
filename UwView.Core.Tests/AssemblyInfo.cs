// テストは直列に走らせる。
//
// いくつかのテストは<b>プロセス全体の状態</b>（カレントディレクトリ・環境変数
// UWVIEW_SYSTEM_ZLIB）を一時的に変える。並列だと、その間に走った別のテストが
// 巻き込まれて落ちる——実際に「重複は先に出た側を採る」「gz のオプション比較」
// などが不定期に失敗していた（2026-09-24）。
// 直列でも全体で十数秒なので、速さより「落ちたら本当に壊れている」を採る。
[assembly: Xunit.CollectionBehavior(DisableTestParallelization = true)]
